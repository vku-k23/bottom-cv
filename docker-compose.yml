version: "3.9"

services:

# existing mysql service on server
  # mysql:
  #   image: mysql:8
  #   container_name: bottom-cv-mysql
  #   restart: always
  #   ports:
  #     - "3306:3306"
  #   environment:
  #     MYSQL_DATABASE: bottom_cv
  #     MYSQL_ROOT_PASSWORD: root
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 5s
  #     timeout: 10s
  #     retries: 10
  #   networks:
  #     - bottom-cv-network

  bottom-cv:
    image: vietquoc2408/bottom-cv:0.0.2-beta
    container_name: bottom-cv
    restart: always
    ports:
      - "8088:8088"
    # depends_on:
    #   mysql:
    #     condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/bottom_cv?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      SERVER_PORT: 8088
    networks:
      - cinevibe-network

# volumes:
#   mysql_data:

networks:
  cinevibe-network:
    driver: bridge
